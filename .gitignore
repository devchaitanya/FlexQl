# Build artifacts
bin/
build/

# Runtime data (WAL log and snapshots are generated on server start)
data/wal/wal.log
data/snapshots/snap_*.bin

# Bench SQLite DB (generated by the reference server)
bench/flexql.db

# Assignment PDFs in extra/ (large binary files)
extra/

# Generated by clangd tooling
compile_commands.json

# Clangd index cache
.cache/

# Claude Code local settings
.claude/

# Deep-research / scratch files
deep-research-report.md
project_dl_lab_CLEAN.pdf

# LaTeX generated artifacts (keep only the .tex source)
docs/*.aux
docs/*.log
docs/*.out
docs/*.toc
docs/*.fls
docs/*.fdb_latexmk
docs/*.synctex.gz
docs/*.pdf

# OS / editor noise
.DS_Store
*.swp
*.swo
*~
